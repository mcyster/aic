[package]
name = "aic"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Command-line access to agentic services"

[dependencies]
clap = { version = "4.5", features = ["derive"] }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
